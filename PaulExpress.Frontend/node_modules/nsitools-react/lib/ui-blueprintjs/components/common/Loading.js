var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import * as React from "react";
import { Spinner, Tag, Intent } from "@blueprintjs/core";
var Loading = /** @class */ (function (_super) {
    __extends(Loading, _super);
    function Loading() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    Loading.prototype.render = function () {
        if (this.props.isLoading) {
            return (React.createElement("div", { style: this.props.spinnerContainerStyle },
                React.createElement(Spinner, null)));
        }
        else {
            return this.props.error ? (React.createElement(Tag, { intent: Intent.DANGER, fill: true }, this.props.error instanceof Error ? this.props.error.message : this.props.error)) : (this.props.children);
        }
    };
    Loading.defaultProps = {
        isLoading: false
    };
    return Loading;
}(React.Component));
export { Loading };
