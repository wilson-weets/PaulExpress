import { IFormGroupProps } from "@blueprintjs/core";
import * as Yup from "yup";
export interface IBaseField extends Pick<IFormGroupProps, "disabled" | "label" | "labelInfo" | "style"> {
    requiredMark?: boolean;
    placeholder?: string;
    validate?: Yup.Schema<any>;
    name?: string;
}
