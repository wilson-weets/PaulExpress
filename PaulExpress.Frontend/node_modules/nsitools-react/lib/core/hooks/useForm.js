var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { useState } from "react";
export var useForm = function (initialValues) {
    var _a = __read(useState(initialValues), 2), values = _a[0], setValues = _a[1];
    var setExplicitValue = function (key, val) {
        var _a;
        setValues(__assign(__assign({}, values), (_a = {}, _a[key] = val, _a)));
    };
    return [
        values,
        function (e) {
            var _a;
            var value;
            if (e.target && e.target.type === "checkbox") {
                value = e.currentTarget.checked;
            }
            else {
                value = e.target.value;
            }
            setValues(__assign(__assign({}, values), (_a = {}, _a[e.target.name] = value, _a)));
        },
        setExplicitValue
    ];
};
