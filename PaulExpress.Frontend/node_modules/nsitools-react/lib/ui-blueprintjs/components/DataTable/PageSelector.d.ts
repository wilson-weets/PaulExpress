import * as React from "react";
import { useGridState } from "../../../core";
interface IPagerSelectorProps {
    tableState: ReturnType<typeof useGridState>;
}
export declare const PagerSelector: React.FunctionComponent<IPagerSelectorProps>;
export {};
