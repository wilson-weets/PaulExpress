export function filterList(data) {
    var objectKeys = Object.keys(data);
    data.map(function (obj) { return ({
        filterText: objectKeys.map(function (k) { return obj[k] + ""; }).join(),
        item: obj
    }); });
}
