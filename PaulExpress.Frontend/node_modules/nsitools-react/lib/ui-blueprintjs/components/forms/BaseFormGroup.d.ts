import * as React from "react";
import { FormikProps } from "formik";
import { Intent } from "@blueprintjs/core";
interface IRenderPropTypes {
    intent: Intent;
    formikProps: FormikProps<any>;
    name: string;
}
export interface IBaseFormGroupProps {
    inline?: boolean;
    helperText?: string;
    formikProps?: FormikProps<any>;
    errorText?: string;
    name?: string;
    label?: string;
    labelInfo?: string;
    style?: React.CSSProperties;
    requiredMark?: boolean;
    showColon?: boolean;
    children: (props: IRenderPropTypes) => React.ReactNode;
}
export declare const BaseFormGroup: React.FunctionComponent<IBaseFormGroupProps>;
export {};
