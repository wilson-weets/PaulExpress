var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import * as React from "react";
import { InputGroup, Button, Intent } from "@blueprintjs/core";
import { BaseFormGroup } from "./BaseFormGroup";
import { IconNames } from "@blueprintjs/icons";
import { getIn } from "formik";
export var TextInputField = function (props) {
    var showEraser = props.showEraser, placeHolder = props.placeHolder, fill = props.fill, disabled = props.disabled, rightElement = props.rightElement, _a = props.autoComplete, autoComplete = _a === void 0 ? false : _a, maxLength = props.maxLength, customOnChange = props.customOnChange, formGroupProps = __rest(props, ["showEraser", "placeHolder", "fill", "disabled", "rightElement", "autoComplete", "maxLength", "customOnChange"]);
    return (React.createElement(BaseFormGroup, __assign({}, formGroupProps), function (_a) {
        var intent = _a.intent, formikProps = _a.formikProps, name = _a.name;
        return (React.createElement(InputGroup, { name: name, intent: intent, fill: fill, maxLength: maxLength, placeholder: placeHolder, value: getIn(formikProps.values, name, ""), onChange: customOnChange || formikProps.handleChange, onBlur: formikProps.handleBlur, disabled: disabled, autoComplete: autoComplete ? "" : "nope", rightElement: rightElement ? (rightElement) : showEraser ? (React.createElement(Button, { icon: IconNames.ERASER, minimal: true, intent: Intent.DANGER, onClick: function () { return formikProps.setFieldValue(name, ""); } })) : (undefined) }));
    }));
};
